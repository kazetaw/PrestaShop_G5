import { test, expect } from '@playwright/test';

test('test', async ({ page }) => {
  await page.goto('http://localhost:8080/th/');
  await page.getByRole('link', { name: 'Vocher 100' }).nth(2).click();
  await page.getByRole('button', { name: ' หยิบใส่ตะกร้า' }).click();
  await page.getByRole('link', { name: ' ทำรายการชำระเงิน' }).click();
  await page.getByRole('link', { name: 'Issue a tax invoice' }).click();
  await page.getByLabel('Mr.').check();
  await page.getByLabel('ชื่อ').click();
  await page.getByLabel('ชื่อ').fill('Suphawit');
  await page.getByLabel('นามสกุล').click();
  await page.getByLabel('นามสกุล').fill('Chamras');
  await page.getByLabel('อีเมลล์').click();
  await page.getByLabel('อีเมลล์').fill('suphawit.c@kkumail.com');
  await page.getByText(' I agree to the terms and').click();
  await page.getByText(' Customer data privacyThe').click();
  await page.getByRole('button', { name: 'ทำต่อ' }).click();
  await page.getByLabel('ประเทศ').selectOption('21');
  await page.getByLabel('บริษัท').click();
  await page.getByLabel('บริษัท').fill('CSKKU');
  await page.getByLabel('ที่อยู่').click();
  await page.getByLabel('ที่อยู่').fill('วิทยาลัยการคอมพิวเตอร์ 123 ถ.มิตรภาพ');
  await page.getByLabel('ที่อยู่').press('Tab');
  await page.getByLabel('Address Complement').fill('ต.ในเมือง อ.เมือง');
  await page.getByLabel('Address Complement').press('Tab');
  await page.getByLabel('จังหวัด').fill('จ.ขอนแก่น');
  await page.getByLabel('รัฐ').selectOption('1');
  await page.getByLabel('รหัสไปรษณีย์').click();
  await page.getByLabel('รหัสไปรษณีย์').fill('40002');
  await page.getByLabel('โทรศัพท์').click();
  await page.getByLabel('โทรศัพท์').fill('0812345678');
  await page.getByRole('button', { name: 'ทำต่อ' }).click();
  await page.getByLabel('Bank transfer').check();
  await page.getByLabel('QR Code').check();
  await page.getByLabel('Promtpay').check();
  await page.locator('#javascript').nth(1).check();
  await page.setInputFiles('#slipFile', 'C:/Users/computer/Downloads/test/imgs/Screenshot 2023-07-06 162012.png');
  const $photoUpload = await page.$('#slipFile');
  const $photoUploadParent = await $photoUpload.$('xpath=..');
  const $photoUploadFigure = await $photoUploadParent.$$('figure');
});