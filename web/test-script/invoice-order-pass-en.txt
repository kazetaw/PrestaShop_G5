import { test, expect } from '@playwright/test';

test('test', async ({ page }) => {
  await page.goto('http://localhost:8080/en/');
  await page.getByRole('link', { name: 'Vocher 100' }).nth(2).click();
  await page.getByRole('button', { name: ' Add to cart' }).click();
  await page.getByRole('link', { name: ' Proceed to checkout' }).click();
  await page.getByRole('link', { name: 'Issue a tax invoice' }).click();
  await page.getByLabel('Mr.').check();
  await page.getByLabel('First name').click();
  await page.getByLabel('First name').fill('Suphawit');
  await page.getByLabel('First name').press('Tab');
  await page.getByLabel('Last name').fill('Chamras');
  await page.getByLabel('Last name').press('Tab');
  await page.getByLabel('Email').fill('suphawit.c@kkumail.com');
  await page.getByText(' I agree to the terms and').click();
  await page.getByText(' Customer data privacyThe').click();
  await page.getByRole('button', { name: 'Continue' }).click();
  await page.getByLabel('Company').click();
  await page.getByLabel('Company').click();
  await page.getByLabel('Company').fill('College of Computing Khon Kaen University');
  await page.getByLabel('Company').press('Tab');
  await page.getByLabel('Address', { exact: true }).click();
  await page.getByLabel('Address', { exact: true }).fill('Tambon Sila');
  await page.getByLabel('Address Complement').click();
  await page.getByLabel('Address Complement').fill('Mueang Khon Kaen District');
  await page.getByLabel('City').click();
  await page.getByLabel('City').fill('Khon Kaen');
  await page.getByLabel('State').selectOption('4');
  await page.getByLabel('Zip/Postal Code').click();
  await page.getByLabel('Zip/Postal Code').fill('40002');
  await page.getByLabel('Phone').click();
  await page.getByLabel('Phone').fill('0812345678');
  await page.getByRole('button', { name: 'Continue' }).click();
  await page.getByLabel('Payment Methods').check();
  await page.setInputFiles('#slipFile', 'C:/Users/computer/Downloads/test/imgs/testimg1.png');
  const $photoUpload = await page.$('#slipFile');
  const $photoUploadParent = await $photoUpload.$('xpath=..');
  const $photoUploadFigure = await $photoUploadParent.$$('figure');
  await page.getByRole('button', { name: 'Upload' }).click();
});